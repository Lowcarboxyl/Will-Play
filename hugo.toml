baseURL = 'https://lowcarboxyl.github.io/Will-Play/'
locale = 'en-us'
title = 'William Ariton Blog'

[ Roland ]  # Just adding a comment context
canonifyURLs = false
relativeURLs = true

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true